vaadin.launch-browser=true
spring.application.name=demo

# Server Configuration
server.port=8080
server.servlet.context-path=/hiss

# Claude AI API Configuration
claude.api.key=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
claude.api.url=https://api.anthropic.com/v1

# Thymeleaf Configuration (if using Thymeleaf for server-side rendering)
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8

# Logging Configuration
logging.level.root=INFO
logging.level.com.example=DEBUG

# Spring Boot Actuator Configuration (optional)
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Database Configuration (if using a database)
# spring.datasource.url=jdbc:mysql://localhost:3306/hiss_db
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# CORS Configuration (if your frontend and backend are on different domains)
spring.webflux.cors.allowed-origins=*
spring.webflux.cors.allowed-methods=GET,POST,PUT,DELETE
spring.webflux.cors.allowed-headers=*

# File Upload Configuration (if handling file uploads)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB